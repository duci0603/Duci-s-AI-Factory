# Project Log - 2026-06-20

## Current Phase

P4-A: OpenClaw local sandbox link verification decision.

## Decisions

- User selected Option A: local sandbox mode.
- This phase verifies whether the OpenClaw local sandbox flow can safely pass through.
- This phase does not test a real project.
- This phase does not connect real message channels.
- This phase does not allow OpenClaw to automatically call Codex to modify files.

## Boundaries

- Use personal-only `~/.openclaw`.
- Bind gateway only to `127.0.0.1:18789`.
- Do not install daemon / LaunchAgent.
- Do not connect channels such as WeChat, Telegram, Slack, or personal Feishu.
- Do not use or restore company `cc-connect`.
- Do not expose the gateway to the public internet.
- Use only personal API/model authorization.
- Start manually, stop after testing.

## Task Tree

```text
P4-A Local Sandbox Link Verification
├─ A0 Decision confirmation: done
├─ A1 Preflight read-only check: path, version, port, ~/.openclaw, cc-connect cleanup state
├─ A2 Sandbox boundary confirmation: loopback, no daemon, no channels, no public exposure, no company config
├─ A3 Onboarding: personal local config only; API/model authorization confirmed or entered by user
├─ A4 Manual gateway start: tmux gateway window, no startup persistence
├─ A5 Health checks: status, gateway status, lsof, channels, sessions
├─ A6 Minimal safety test: local request/status only, no real engineering task
└─ A7 Closeout: stop gateway, confirm port closed, update manual and logs
```

## Node Checks

- A1 succeeds when OpenClaw CLI is available, port 18789 status is clear, `~/.openclaw` state is known, and company `cc-connect` is not executable.
- A2 succeeds when loopback/no-daemon/no-channels/no-public/no-company-config boundaries are confirmed.
- A3 succeeds when only personal local configuration is created and no company credentials or channels are written.
- A4 succeeds when gateway can be started manually in the visible gateway window.
- A5 succeeds when status, gateway status, port, channels, and sessions are readable.
- A6 succeeds when a harmless local check completes without modifying files or running a real project.
- A7 succeeds when gateway is stopped, port is closed, and records are updated.

## Next Step

Execute A1 preflight read-only check only:

- Check `openclaw` path and version.
- Check Node/npm version.
- Check whether port `18789` is listening.
- Check `~/.openclaw` state.
- Confirm company `cc-connect` remains uninstalled and non-executable.

Do not run `openclaw onboard`.
Do not start gateway.
Do not install daemon.
Do not connect channels.

## P4-A A1-A4 Execution Update

### A1 Preflight Read-Only Check

- `openclaw` path: `/opt/homebrew/bin/openclaw`.
- OpenClaw version: `OpenClaw 2026.6.8 (844f405)`.
- Node/npm: Node `v26.3.0`, npm `11.16.0`.
- Port `18789`: not listening.
- `cc-connect`: not executable; global package uninstalled.
- LaunchAgent / daemon: not installed.
- Channels: none configured.

### A2 Sandbox Boundary

- Mode: local.
- Gateway bind: loopback only.
- Port: `18789`.
- Tailscale: off.
- Auth: token.
- No daemon.
- No channels.
- No public exposure.
- No company `cc-connect`.
- No automatic Codex file modification.

### A3 Minimal Onboarding

- Generated `~/.openclaw/openclaw.json`.
- Created `~/.openclaw/workspace`.
- Created `~/.openclaw/agents/main/sessions`.
- Configured `gateway.mode=local`, `gateway.bind=loopback`, `gateway.port=18789`, `gateway.tailscale.mode=off`.
- Configured token auth through env SecretRef `OPENCLAW_GATEWAY_TOKEN`.
- Did not write model API key.
- Did not start gateway.
- Did not install daemon.
- Did not configure channels.

### A4 Manual Start Plan

Command to run only after user confirmation, preferably in the `tmux` gateway window:

```bash
export OPENCLAW_GATEWAY_TOKEN="$(openssl rand -hex 32)"
openclaw gateway run --bind loopback --port 18789 --auth token --tailscale off
```

Check from another window:

```bash
lsof -nP -iTCP:18789 -sTCP:LISTEN
openclaw gateway status
```

Stop with `Ctrl+C`, then confirm the port is closed.

## P4-A A4 Manual Gateway Verification Result

Completed on 2026-06-23 after user confirmation.

### What Ran

- Started OpenClaw gateway manually in the visible `tmux` `gateway` window.
- Used foreground mode only.
- Used loopback bind on port `18789`.
- Used token auth.
- Kept Tailscale off.
- Did not install daemon / LaunchAgent.
- Did not connect channels.
- Did not use company `cc-connect`.
- Did not allow OpenClaw to call Codex for file modification.

### Verification Results

- Gateway reached ready state.
- `lsof` confirmed listeners only on `127.0.0.1:18789` and `[::1]:18789`.
- No public listener was found.
- `openclaw gateway status` showed LaunchAgent not loaded and service not installed.
- `launchctl list` did not show OpenClaw / cc-connect / claw background items.
- Gateway was stopped with `Ctrl+C` and logged clean shutdown.
- After shutdown, `lsof -nP -iTCP:18789 -sTCP:LISTEN` returned no listener.

### Expected Runtime Error

Gateway logs showed `No API key found for provider "openai"`. This is expected because A3 intentionally did not write a model API key or model authorization into OpenClaw config. It does not invalidate the A4 gateway start/listen/stop verification, but real model tasks cannot run yet.

### Result

A4 passed. OpenClaw gateway can be started manually in local sandbox mode, listens only on loopback, does not leave a service installed, and can be stopped cleanly.

### Next Step

Proceed to A5 read-only health checks only: read status, read gateway status, confirm port state, confirm channels remain unconfigured, and confirm sessions state. Do not send real external messages, run real engineering tasks, or validate model invocation until personal model/API authorization is explicitly configured.
