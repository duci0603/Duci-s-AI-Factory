# Project Log - 2026-06-23

## Current Phase

P4-A: OpenClaw local sandbox link verification.

## A4 Manual Gateway Start Verification

Completed A4 after user confirmation.

### What Ran

- Started OpenClaw gateway manually in the visible `tmux` `gateway` window.
- Used foreground mode only.
- Used loopback bind.
- Used port `18789`.
- Used token auth.
- Kept Tailscale off.
- Did not install daemon / LaunchAgent.
- Did not connect channels.
- Did not use company `cc-connect`.
- Did not allow OpenClaw to call Codex for file modification.

### Verification Results

- Gateway reached ready state.
- `lsof` confirmed listeners only on:
  - `127.0.0.1:18789`
  - `[::1]:18789`
- No public listener was found.
- `openclaw gateway status` showed LaunchAgent not loaded and service not installed.
- `launchctl list` did not show OpenClaw / cc-connect / claw background items.

### Expected Runtime Error

Gateway logs showed `No API key found for provider "openai"`.

This is expected because A3 intentionally did not write a model API key or model authorization into OpenClaw config. This does not invalidate the A4 gateway start/listen/stop verification, but it means real model tasks cannot run yet.

### Shutdown

- Stopped the gateway with `Ctrl+C`.
- Gateway logged clean shutdown.
- After shutdown, `lsof -nP -iTCP:18789 -sTCP:LISTEN` returned no listener.

## Result

A4 passed.

OpenClaw gateway can be started manually in local sandbox mode, listens only on loopback, does not leave a service installed, and can be stopped cleanly.

## Next Step

Proceed to A5 read-only health checks only:

- Read status.
- Read gateway status.
- Confirm port state.
- Confirm channels remain unconfigured.
- Confirm sessions state.
- Do not send real external messages.
- Do not run real engineering tasks.
- Do not validate model invocation until personal model/API authorization is explicitly configured.

## A5 Read-Only Health Checks

Completed A5 after A4 was documented and committed.

### Commands

Ran only read-only status checks:

- `openclaw status`
- `openclaw gateway status`
- `openclaw channels status`
- `openclaw sessions list`
- `lsof -nP -iTCP:18789 -sTCP:LISTEN`
- `launchctl list` filtered for OpenClaw / cc-connect / claw

### Results

- Gateway target remains local loopback: `ws://127.0.0.1:18789`.
- Gateway is currently unreachable with `ECONNREFUSED`, which matches the expected stopped state after A4.
- Tailscale exposure remains off.
- Gateway LaunchAgent is not installed / not loaded.
- Node LaunchAgent is not installed.
- Port `18789` has no listener.
- No OpenClaw / cc-connect / claw background item was found in `launchctl list`.
- Channels: no channels configured.
- Sessions: one local direct session exists: `agent:main:main`, shown as OpenAI Codex runtime. This is local OpenClaw session state, not a real message-channel connection.
- Gateway token remains a SecretRef; because `OPENCLAW_GATEWAY_TOKEN` was not set in the command path, status ran in degraded read-only config. No token was written or exposed.

### Sandbox Note

Inside the Codex sandbox, some OpenClaw CLI status commands failed with SQLite readonly database / chmod permission errors. The same read-only checks were rerun in the authorized environment and produced the status above.

### Result

A5 passed. Gateway is stopped, port is closed, no daemon or LaunchAgent is installed, channels remain unconfigured, and no company `cc-connect` channel is active.

### Next Step

A6, if approved later, should remain a minimal local safety test only. Do not connect real message channels, do not run real engineering tasks, and do not configure model/API authorization without explicit confirmation.


## A6 Minimal Local Safety Test Plan

Created the A6 plan only. Did not run the gateway, configure credentials, connect channels, or execute any OpenClaw task.

### Objective

A6 should verify only the minimal local request/status path for a manually started loopback gateway. It must not validate model ability, real project execution, message delivery, or Codex file modification.

### Boundaries

- User confirmation is required before A6-Run.
- Gateway may only run in foreground mode in the visible `tmux` `gateway` window.
- Bind must remain loopback on port `18789`.
- Tailscale exposure remains off.
- Token auth may use only a temporary `OPENCLAW_GATEWAY_TOKEN` environment variable.
- Do not write or expose the token.
- Do not configure model API keys.
- Do not install daemon / LaunchAgent.
- Do not connect channels.
- Do not send external messages.
- Do not run real engineering tasks.
- Do not allow OpenClaw to call Codex to modify files.

### Draft Commands For Later Confirmation

```bash
export OPENCLAW_GATEWAY_TOKEN="$(openssl rand -hex 32)"
openclaw gateway run --bind loopback --port 18789 --auth token --tailscale off
```

Read-only checks from another window:

```bash
openclaw gateway status
openclaw status
lsof -nP -iTCP:18789 -sTCP:LISTEN
openclaw channels status
openclaw sessions list
```

### Success Criteria

- Gateway starts manually in foreground mode.
- Listener remains loopback-only; no public `0.0.0.0` listener.
- Local status/probe/health checks are readable or fail safely with a clear auth/config reason.
- Channels remain unconfigured.
- No external messages are sent.
- Codex does not modify files through OpenClaw.
- Gateway is stopped after the test.
- Port `18789` is closed after shutdown.
- No OpenClaw / cc-connect / claw background service remains in `launchctl list`.

### Next Step

Wait for explicit user confirmation before A6-Run.
