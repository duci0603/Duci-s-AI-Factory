# Project Log - 2026-06-16

## Current Phase

P1: GitHub standardization.

## Completed

- Added a formal naming rules chapter to `docs/AI_FACTORY_MANUAL.md`.
- Added naming rules to `AGENTS.md` for future Codex/OpenClaw execution.
- Naming rules now cover:
  - project and repository names
  - repository directory names
  - core file names
  - current repository structure and file purpose
  - Word manual names
  - project log names
  - tmux session and window names
  - script and tool names
  - Git branch names
  - Git commit message style
  - OpenClaw/Codex task names
- Added and verified a new GitHub SSH authentication key:
  - `Mac mini Codex AI Factory`
- Successfully pushed the P1 standardization commit to GitHub using the new SSH key.
- Recovered the original SSH key by adding it to macOS Keychain/ssh-agent.
- Created `~/.ssh/config` so `github.com` uses the original key by default:
  - `~/.ssh/id_ed25519`
- Verified default GitHub SSH authentication with `ssh -T git@github.com`.
- Verified normal Git access with `git fetch origin`.

## Issues

- `.DS_Store` remains a local ignored file and is not tracked by Git.

## Next Step

1. Use the original SSH key as the default GitHub identity.
2. Continue with tmux standardization and later deployment phases.
3. Keep the new `id_ed25519_codex` key as a fallback or remove it later after confirmation.
